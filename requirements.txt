# Indian Finance Scraper — Python Dependencies
# Install with: pip install -r requirements_scraper.txt

# Core scraping
requests==2.31.0
beautifulsoup4==4.12.3
lxml==5.2.2                    # fast HTML parser for BS4

# JS-heavy sites
playwright==1.44.0             # after install, run: playwright install chromium

# Embeddings + Vector DB (same as your RAG pipeline)
chromadb==0.5.0
sentence-transformers==2.7.0

# Utilities
python-dotenv==1.0.1
numpy==1.26.4

# Optional: speeds up text parsing
html5lib==1.1